git init # Init un projet git sur son pc
git add . # Selection des fichier à envoyer 
git commit -m "📦 NEW: Premier commit by Sayna" # Création d'un package avant envoi
git branch -M main # Création de la branch principal 'Main'
git remote add origin https://github.com/Sylvestre22/sayna-html-css-exercice1.git # Link entre le code sur le pc avec les serveurs de GitHub
git push -u origin main # Envoi du package

git add . # Selection des fichier à envoyer (. => Tout)
git commit -m "..." # Création d'un package avant envoi
git push # Envoi